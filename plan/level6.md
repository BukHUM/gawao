# 🗞️ Level 6: นักเล่าข่าว (The Content Scraper)

ยินดีต้อนรับสู่ขั้นสูงสุดครับ! ใน Level นี้เราจะเปลี่ยนระบบจากการ "สุ่มค้นหา" เป็นการ **"เจาะลึกข่าวต้นฉบับ"** เพื่อความแม่นยำ 100%

---

## 🗺️ แผนผังการทำงาน (Workflow ใหม่)
เราจะรื้อช่วงต้นน้ำนิดหน่อยครับ:
`HTTP Request` (ดึง RSS) ➡️ `XML` (แกะโค้ด) ➡️ `Item Lists` (แยกข่าว) ➡️ `Code` (เอาลิ้งค์จริง) ➡️ **`HTTP Request` (ดูดเว็บ)** ➡️ `HTML to Markdown` ➡️ `AI Rewrite`

---

## 🛠️ ขั้นตอนการสร้าง (Step-by-Step)

### Step 1: อ่าน RSS แบบดิบๆ (Raw Feed)
ลบ Node **RSS Read** เดิมออก แล้วใส่ 3 Node นี้แทนครับ:

1. **HTTP Request**
   - **Method:** `GET`
   - **URL:** `https://trends.google.com/trending/rss?geo=TH`
   - **Response Format:** `String` (สำคัญ!)

2. **XML** (ต่อจาก HTTP Request)
   - **Mode:** `Convert XML to JSON`
   - **JSON Property Name:** `data` (ตั้งชื่อตัวแปรว่า data)

3. **Item Lists** (ต่อจาก XML)
   - **Resource:** `Item`
   - **Operation:** `Split Out Items`
   - **Field To Split Out:** `data.rss.channel.item`
   - (กด Execute ดู จะต้องเห็นข่าวแยกออกมาเป็น item ครับ)

### Step 2: ดึงลิ้งค์ข่าวจริง (Extract Real Link)
เพิ่ม Node **Code** ต่อจาก Item Lists แล้วใส่โค้ดนี้:

```javascript
// ดึง Link ข่าวอันแรกจาก List
const newsItems = item.json['ht:news_item'];
const realLink = newsItems ? newsItems[0]['ht:news_item_url'] : "";

return {
  json: {
    title: item.json.title,
    original_link: realLink,
    pubDate: item.json.pubDate
  }
}
```

### Step 3: นักดูดเนื้อหา (Scraper) 🕷️
ตอนนี้เราได้ Link จริงแล้ว (`original_link`) ให้ส่งไปดูดเนื้อหาครับ:

1. **HTTP Request** (ต่อจาก Code)
   - **URL:** `{{ $json.original_link }}`
   - **Method:** `GET`
   - **Response Format:** `String`
   - **On Error:** `Continue` (เผื่อเว็บไหนดูดไม่ได้ ให้ข้ามไป ไม่ต้องหยุด Workflow)

2. **HTML to Markdown** (แนะนำสุดๆ)
   - Node นี้จะช่วยแปลง HTML รกๆ ให้เป็น Text ล้วนๆ ที่ AI อ่านง่าย
   - **Input Field:** `data` (หรือ field ที่ HTTP Request ส่งมา)

### Step 4: นักเขียน (AI Rewrite) ✍️
ส่ง Text ที่ได้ไปให้ AI Agent หรือ Basic LLM Chain เขียนใหม่ครับ

**Prompt แนะนำ:**
> "นี่คือเนื้อหาข่าวต้นฉบับ:
> {{ $json.data }} (หรือ text จาก node ก่อนหน้า)
>
> ภารกิจ: เขียนข่าวนี้ใหม่ (Rewrite) เป็นภาษาไทยให้น่าอ่าน สนุก กระชับ เหมือนเล่าให้เพื่อนฟัง
> - ตั้งชื่อเรื่องใหม่ให้น่าสนใจ
> - สรุปใจความสำคัญ
> - ห้ามคัดลอกมาทั้งดุ้น ต้องเขียนใหม่ด้วยสำนวนใหม่"

---

## ✅ ข้อดีของวิธีนี้
1. **ได้เนื้อหาชัวร์:** ไม่ต้องลุ้นว่า Search เจอไหม
2. **ข้อมูลถูกต้อง:** มาจากข่าวต้นฉบับเป๊ะๆ
3. **ไม่ซ้ำใคร:** เพราะเรา Rewrite ใหม่ด้วย AI

**ลองต่อตามนี้แล้ว Execute ดูครับ ถ้าติดตรงไหนดึงข่าวไม่ได้ บอกผมเลย!** 🚀