# 📘 คู่มือโปรเจกต์: สร้าง AI นักข่าวทันกระแส (Level 3)

ยินดีต้อนรับสู่ Level 3 ครับ! ขั้นตอนนี้คือศิลปะของการใช้ AI เราจะเปลี่ยน "ข้อมูลแห้งๆ" ให้เป็น "บทความคุณภาพ" ที่พร้อมขึ้นหน้าเว็บ

---

## 🎯 Level 3: บรรณาธิการ (Content Creation)

### 1. เป้าหมายของ Level นี้ 📝

เราต้องการให้ AI รับข้อมูลจาก Level 2 แล้วทำสิ่งต่อไปนี้:
- **คิดชื่อเรื่องใหม่:** ให้ดึงดูดใจ (Clickbait นิดๆ ให้คนอยากกด)
- **เขียนเนื้อหาใหม่:** ปรับสำนวนให้อ่านง่าย สนุก มีลูกเล่น (Emoji)
- **จัด Format:** ใส่ HTML Tag (`<h2>`, `<p>`, `<ul>`) เพื่อให้เวลาส่งไป WordPress แล้วแสดงผลสวยเลย ไม่ต้องไปจัดหน้าเอง

---

## 2. ผังการทำงาน (Workflow)

ภาพรวมจะต่อจากเดิมครับ:
`AI Agent` (นักสืบ) ➡️ `Basic LLM Chain` (บรรณาธิการ)

> **💡 ทำไมต้องแยก Node?**
> เราแยก Node กันเพื่อไม่ให้ Prompt ตีกัน นักสืบก็สืบไป นักเขียนก็เขียนไป เป็นการแบ่งหน้าที่ (Separation of Concerns) จะทำให้งานออกมาดีกว่าจับฉ่ายรวมกันครับ

---

## 3. ขั้นตอนการสร้าง (Step-by-Step)

### Step 1: เตรียม Node นักเขียน
1. ต่อเส้นจาก Node **AI Researcher Agent** (Output จาก Level 2)
2. เพิ่ม Node ชื่อ **Basic LLM Chain**
   - *คำอธิบาย:* Node นี้เหมาะมากสำหรับงานประเภท "รับ Text มา แล้วแปลงเป็น Text ใหม่"

### Step 2: เชื่อมต่อสมอง (Model)
1. ลากเส้นจากช่อง **Model** ของ **Basic LLM Chain** ไปหา Node **Google Gemini Chat Model** ตัวเดิม (หรือสร้างใหม่ก็ได้)
2. **ประหยัดทรัพยากร:** ใช้ Model ตัวเดิมจาก Level 2 มาเสียบได้เลยครับ

### Step 3: เขียนคำสั่ง (Prompt Engineering) ✍️ **สำคัญที่สุด**
ใน Node **Basic LLM Chain** ให้ตั้งค่า Prompt ดังนี้:

**Prompt:**
```text
คุณคือนักเขียนข่าวออนไลน์มืออาชีพ ที่เชี่ยวชาญการเล่าเรื่องให้น่าสนใจและทันสมัย

ข้อมูลดิบ:
{{ $json.output }}

คำสั่ง:
ช่วยเขียนบทความข่าวจากข้อมูลดิบด้านบน โดยมีโครงสร้างดังนี้:
1. พาดหัวข่าว (Title): ตั้งให้ดึงดูด น่าคลิกอ่าน และมี Emoji
2. เนื้อหา (Content): เขียนสรุปให้อ่านง่าย แบ่งย่อหน้าชัดเจน ใช้ภาษาที่เป็นกันเอง
   - ให้ส่งออก (Output) เฉพาะส่วน Content ในรูปแบบ HTML (ใช้ tag <p>, <h2>, <ul>, <li> ได้)
   - ไม่ต้องใส่ tag <html> หรือ <body> ครอบ

รูปแบบ Output JSON ที่ต้องการ:
{
  "title": "พาดหัวข่าวที่คุณคิด",
  "content": "เนื้อหาข่าวในรูปแบบ HTML..."
}
ตอบกลับมาเป็น JSON เท่านั้น ไม่ต้องมีเกริ่นนำ
```

---

## 4. แจก Workflow Code (Level 3 Template)

ก๊อปปี้ Code นี้ไปวางต่อจาก Node Agent ใน Level 2 ได้เลยครับ

```json
{
  "meta": {
    "instanceId": "generated_by_gemini_level3"
  },
  "nodes": [
    {
      "parameters": {
        "prompt": "=คุณคือนักเขียนข่าวออนไลน์มืออาชีพ ที่เชี่ยวชาญการเล่าเรื่องให้น่าสนใจและทันสมัย\n\nข้อมูลดิบ:\n{{ $json.output }}\n\nคำสั่ง:\nช่วยเขียนบทความข่าวจากข้อมูลดิบด้านบน โดยมีโครงสร้างดังนี้:\n1. พาดหัวข่าว (Title): ตั้งให้ดึงดูด น่าคลิกอ่าน และมี Emoji\n2. เนื้อหา (Content): เขียนสรุปให้อ่านง่าย แบ่งย่อหน้าชัดเจน ใช้ภาษาที่เป็นกันเอง\n   - ให้ส่งออก (Output) เฉพาะส่วน Content ในรูปแบบ HTML (ใช้ tag <p>, <h2>, <ul>, <li> ได้)\n   - ไม่ต้องใส่ tag <html> หรือ <body> ครอบ\n\nรูปแบบ Output JSON ที่ต้องการ:\n{\n  \"title\": \"พาดหัวข่าวที่คุณคิด\",\n  \"content\": \"เนื้อหาข่าวในรูปแบบ HTML...\"\n}\nตอบกลับมาเป็น JSON เท่านั้น ไม่ต้องมีเกริ่นนำ"
      },
      "id": "editor-llm",
      "name": "Editor LLM",
      "type": "@n8n/n8n-nodes-langchain.chainLlm",
      "typeVersion": 1.4,
      "position": [
        1480,
        340
      ]
    },
    {
      "parameters": {},
      "id": "json-parser",
      "name": "Parse JSON",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1700,
        340
      ],
      "jsCode": "// รับค่าจาก Node ก่อนหน้า (รองรับหลายชื่อตัวแปรเผื่อ AI ส่งมาต่างกัน)\nconst inputItem = items[0].json;\nconst aiResponse = inputItem.text || inputItem.output || inputItem.response || inputItem.content || '';\n\nlet parsedData;\n\ntry {\n  // Clean Code Block ถ้ามี\n  const cleanJson = aiResponse.replace(/```json/g, '').replace(/```/g, '').trim();\n  parsedData = JSON.parse(cleanJson);\n} catch (error) {\n  // กรณี Parse ไม่ผ่าน ให้ส่งค่าเดิมออกไป (อย่างน้อยไม่ Empty)\n  parsedData = { \n    title: 'AI Parse Error', \n    content: `<p>Raw output: ${aiResponse}</p><p>Error: ${error.message}</p>` \n  };\n}\n\nreturn [\n  {\n    json: parsedData\n  }\n];"
    }
  ],
  "connections": {
    "Editor LLM": {
      "main": [
        [
          {
            "node": "Parse JSON",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
```

---

## 💡 ทริคเพิ่มเติมสำหรับ Level 3
ถ้าคุณรู้สึกว่าบทความที่ได้ยัง "ไม่แซ่บ" พอ ลองกลับไปแก้ Prompt ใน Node `Editor LLM` ดูครับ เช่น:
* *"ขอสำนวนแบบวัยรุ่น สั้น กระชับ"*
* *"เขียนแบบทางการ สำหรับข่าวธุรกิจ"*
* *"ช่วยใส่ Hashtag ต่อท้ายบทความให้ด้วย"*

**ทดลองเล่นจนพอใจ แล้วบอกผมนะครับถ้าพร้อมจะไป **Level 4** เพื่อยิงขึ้น WordPress!** 🚀
