# 📘 คู่มือโปรเจกต์: สร้าง AI นักข่าวทันกระแส (Level 4 - Final)

ยินดีด้วยครับที่คุณเดินทางมาถึง Level สุดท้าย! ตอนนี้เรามีเนื้อหาข่าวที่ผ่านการ Research และ Rewrite เรียบร้อยแล้ว ขั้นตอนนี้คือการส่งมันขึ้นเว็บไซต์ WordPress ครับ

---

## 🎯 Level 4: สำนักพิมพ์ (Publishing)

### 1. กุญแจสำคัญ: Application Password 🔑

ก่อนจะสร้าง Node คุณต้องมี "กุญแจ" สำหรับไขเข้าหลังบ้าน WordPress ก่อน (ใช้รหัส Login ปกติไม่ได้นะครับ เพื่อความปลอดภัย)

**วิธีสร้าง (ทำบนเว็บ WordPress ของคุณ):**
1. Login เข้า WordPress Admin
2. ไปที่เมนู **Users (ผู้ใช้)** > **Profile (โปรไฟล์)**
3. เลื่อนลงมาด้านล่างสุด หาหัวข้อ **Application Passwords**
4. ตั้งชื่อ (เช่น `n8n-bot`) แล้วกด **Add New**
5. **สำคัญ:** ก๊อปปี้รหัสผ่านยาวๆ ที่ระบบให้มาเก็บไว้ทันที (มันจะโชว์แค่ครั้งเดียว!)

---

## 2. ขั้นตอนการสร้าง (Step-by-Step)

### Step 1: เพิ่ม Node WordPress
1. ต่อเส้นจาก Node **Parse JSON** (ตัวสุดท้ายของ Level 3)
2. เพิ่ม Node ชื่อ **WordPress**
3. **สิ่งที่ต้องเลือกต่อ:** ระบบจะถามว่าจะทำอะไร ให้เลือก **"Create a post"** (หรือ **Post Actions** > **Create a post**)

### Step 2: ตั้งค่าการเชื่อมต่อ (Credentials)
1. สังเกตด้านบนของ Node จะมีช่อง **Credential to connect with**
2. เลือก **Create New**
3. **User:** ใส่ Username ที่ใช้ล็อกอิน WordPress
4. **Password:** ใส่ Application Password ที่เพิ่งสร้างมา (ไม่ใช่รหัสผ่านล็อกอินปกตินะ!)
5. **URL:** ใส่ลิงก์เว็บของคุณ เช่น `https://mywebsite.com` (ต้องมี `https://` และไม่ต้องมี `/wp-admin` ต่อท้าย)
6. กด **Connect** ถ้าขึ้นสีเขียวแปลว่าผ่าน!

### Step 3: ตั้งค่าการโพสต์ (Mapping Data) 🗺️
ตั้งค่าใน Node **WordPress** ดังนี้:
*(ถ้าคุณเลือก Create a post มาแล้ว Resource กับ Operation จะถูกตั้งให้อัตโนมัติครับ)*
- **Title:** กดรูปเฟืองหรือลากเส้น Map ข้อมูล เลือก `{{ $json.title }}`
- **Content:** (ถ้าหาช่อง Content ไม่เจอ) ให้กดปุ่ม **Add Field** ด้านล่าง > เลือก **Content**
    - จากนั้นกดรูปเฟืองหรือลากเส้น Map ข้อมูล เลือก `{{ $json.content }}`
- **Status:** (สำคัญ!) ให้กดปุ่ม **Add Field** > เลือก **Status** (หรือ **Publish Status**)
    - ตั้งค่าเป็น `Draft` (แนะนำให้เป็นฉบับร่างก่อน เพื่อความปลอดภัย) หรือ `Publish` (ถ้ามั่นใจแล้ว)

---

## 3. แจก Workflow Code (Level 4 Template)

ก๊อปปี้ Code นี้ไปต่อท้าย workflow เดิมได้เลยครับ

```json
{
  "meta": {
    "instanceId": "generated_by_gemini_level4"
  },
  "nodes": [
    {
      "parameters": {
        "title": "={{ $json.title }}",
        "content": "={{ $json.content }}",
        "additionalFields": {
          "status": "draft",
          "comment_status": "open"
        }
      },
      "id": "wordpress-node",
      "name": "WordPress Post",
      "type": "n8n-nodes-base.wordpress",
      "typeVersion": 1,
      "position": [
        1920,
        340
      ],
      "credentials": {
        "wordpressApi": {
          "id": "YOUR_WP_CREDENTIAL_ID",
          "name": "WordPress account"
        }
      }
    }
  ],
  "connections": {
    "Parse JSON": {
      "main": [
        [
          {
            "node": "WordPress Post",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  }
}
```

> **หมายเหตุ:** อย่าลืมแก้ตรง `Parse JSON` ในแท็บ Connections ให้ตรงกับชื่อ Node ก่อนหน้าของคุณนะครับ ถ้าก๊อปปี้ไปวางแล้วเส้นไม่เชื่อมกัน

---

### Step 4: ปิดวงจรลูป (Close the Loop) 🔄 **(เพื่อให้ทำครบทุกข่าว)**
ตอนนี้ระบบจะทำงานแค่ข่าวเดียวแล้วหยุด ถ้าต้องการให้ทำครบทุกข่าว ต้องลากเส้นวนกลับครับ:
1. ลากเส้นจาก Node **WordPress Post** (ตัวสุดท้าย)
2. วนกลับไปเชื่อมที่ช่อง **Input** ของ Node **Loop Over Items**
3. เส้นจะโค้งๆ เป็นวงกลม แสดงว่าระบบพร้อมวนลูปแล้ว!

---

## 5. ทดสอบระบบแบบครบวงจร (Full Loop Test) 🚀

มาดูผลงานชิ้นโบว์แดงกันครับ!

1. กด **Execute Workflow** (ปุ่มใหญ่ด้านล่าง)
2. นั่งจิบกาแฟดู Node ทำงานวิ่งเขียวไปทีละตัว:
   - 🟢 **RSS:** ดึงหัวข้อข่าวมา
   - 🟢 **Loop:** หยิบข่าวแรกมาทำ
   - 🟢 **Agent:** วิ่งไป Search ข้อมูล
   - 🟢 **Editor:** เขียนข่าวใหม่ ใส่ HTML
   - 🟢 **WordPress:** ยิงขึ้นเว็บ!
3. เปิดหน้า **Posts** ใน WordPress ของคุณดูครับ...
4. คุณควรจะเห็นบทความใหม่ขึ้นมาในสถานะ **Draft** พร้อมชื่อเรื่องและเนื้อหาที่ AI เขียนให้!

---

## 🎓 บทสรุปและก้าวต่อไป (Graduation)

ยินดีด้วยครับ! คุณเพิ่งสร้าง **"สำนักข่าวอัตโนมัติ"** สำเร็จด้วยมือตัวเอง 🏆

**สิ่งที่ระบบนี้ทำได้:**
- ✅ **Monitoring** เทรนด์ใหม่ๆ ตลอดเวลา
- ✅ **Research** หาข้อมูลเองเหมือนนักข่าว
- ✅ **Rewrite** เขียนข่าวใหม่ไม่ซ้ำใคร
- ✅ **Publish** ส่งงานขึ้นเว็บรอเรากดเผยแพร่

**ไอเดียพัฒนาต่อ (Bonus Challenge):**
- **เพิ่มรูปภาพ:** ให้ AI Agent ค้นหา URL รูปภาพ หรือใช้ AI วาดรูป (เช่น Stable Diffusion) มาประกอบข่าว
- **ตั้งเวลา (Schedule):** เปลี่ยนปุ่มกดมือ เป็น Node **Schedule Trigger** ให้มันทำงานเองทุกๆ 8 โมงเช้า
- **ส่งแจ้งเตือน:** เพิ่ม Node **Line Notify** หรือ **Telegram** ส่งข้อความบอกเราเมื่อมีบทความใหม่เขียนเสร็จแล้ว

**ขอให้สนุกกับการสร้างกองทัพ Automation นะครับ! หากมีคำถามเพิ่มเติมหรืออยากอัปเกรดระบบส่วนไหน ถามได้เสมอครับ!**