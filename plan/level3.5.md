เป็นคำถามที่ดีมากครับ การมีภาพประกอบจะช่วยให้บทความดู "มืออาชีพ" ขึ้นทันที ซึ่งใน n8n เราสามารถทำได้ทั้งแบบ **AI วาดใหม่ (Generate)** และ **ดึงภาพที่มีอยู่แล้ว (Search)** ครับ

สำหรับโจทย์ที่คุณต้องการ **"ใช้ภาพทั่วไปหรือดึงจากเว็บ"** (เหมือน Stock Photo) ผมขอแนะนำให้ทำเป็น **Level 3.5: ช่างภาพ (The Photographer)** แทรกเข้าไประหว่าง "นักสืบ" กับ "บรรณาธิการ" ครับ

---

### 📍 แทรกตรงไหนดี?

ตำแหน่งที่เหมาะสมที่สุดคือ:
`AI Researcher Agent` (ได้เนื้อหา) ➡️ **`Image Search` (หาภาพ)** ➡️ `Editor LLM` (เอาภาพไปประกอบเรื่อง)

### 🛠️ วิธีดึงภาพประกอบ (โดยไม่ต้อง Gen AI)

เราจะใช้ **Google Custom Search** ตัวเดิมที่มีอยู่แล้วครับ แต่เปลี่ยนโหมดเป็นค้นหารูปภาพแทน

#### วิธีทำ (Step-by-Step)

1. **เพิ่ม Node:** ต่อจาก `AI Researcher Agent` ให้เพิ่ม Node **`Google Custom Search`**
2. **ตั้งค่า:**
* **Operation:** `Get Many`
* **Query:** `{{ $('AI Researcher Agent').item.json.output }}` (หรือใช้ Title จาก RSS ก็ได้ครับ เพื่อให้ได้ภาพตรงปก)
* **Options:**
* กด Add Option > **Search Type**: เลือก `Image`
* กด Add Option > **Number of Results**: เลือก `1` (เอาแค่รูปเดียวที่ตรงสุด)
* *(เสริม)* **Image Size**: เลือก `Large` หรือ `XLarge` เพื่อให้ได้ภาพชัดๆ





---

### 🧩 วิธีนำภาพไปใช้ในบทความ

เมื่อได้ Link รูปภาพมาแล้ว เรามี 2 ทางเลือกครับ:

#### ทางเลือก A: แทรกในเนื้อหาเลย (ง่ายที่สุด - แนะนำสำหรับมือใหม่)

เราจะส่ง Link รูปภาพไปให้ `Editor LLM` แล้วสั่งให้มันเขียน HTML Tag `<img src="...">` แทรกเข้าไปในบทความเลย

**ต้องแก้ Prompt ใน Node Editor LLM (Level 3) นิดหน่อยครับ:**

> "เพิ่มคำสั่ง: ฉันมีลิงก์รูปภาพประกอบข่าวนี้ คือ: `{{ $json.link }}` ช่วยแทรกรูปนี้ลงในเนื้อหา (Content) โดยใช้ HTML tag `<img>` วางไว้หลังย่อหน้าแรก และจัดกึ่งกลางให้สวยงาม"

#### ทางเลือก B: ตั้งเป็นภาพปก (Featured Image) บน WordPress (Advance)

วิธีนี้จะยากกว่าหน่อย เพราะ WordPress ไม่ยอมรับ URL ตรงๆ เป็นภาพปก เราต้อง:

1. ใช้ Node `HTTP Request` โหลดไฟล์ภาพมาเป็น Binary
2. ใช้ Node `WordPress` (Upload Media) อัปโหลดไฟล์ขึ้นไปก่อน -> ได้ ID กลับมา
3. เอา ID ไปใส่ในช่อง `Featured Media` ตอนโพสต์

---

### 🎁 แจก Code: Level 3.5 (Image Search)

อันนี้เป็น Code สำหรับ **ทางเลือก A (แทรกในเนื้อหา)** ที่ทำได้ง่ายและรวดเร็วครับ คุณสามารถนำไปแทรกต่อจาก AI Agent ได้เลย


*(หมายเหตุ: อย่าลืม Map ตัวแปร Query ใน Node นี้ให้เป็น Keyword หรือ Title ที่คุณต้องการค้นหาภาพนะครับ)*

### 💡 ทริคเพิ่มเติม: ถ้าไม่อยากใช้ Google Search?

ถ้าอยากได้ภาพสวยๆ แบบอาร์ตๆ (Mockup style) โดยไม่ต้องค้นหา คุณสามารถใช้ **Unsplash** ได้ครับ

* **Node:** `Unsplash` (n8n มีให้ใช้เลย)
* **ข้อดี:** ภาพสวย คมชัด ถูกลิขสิทธิ์ (Royalty Free) มากกว่าการสุ่มหาจาก Google
* **วิธีใช้:** เลือก Operation เป็น `Search` แล้วใส่ Keyword ลงไปเหมือนกันครับ

ลองเลือกวิธีที่ชอบดูนะครับ ถ้าอยากลองแบบ Advance (อัปโหลดเป็นภาพปก Featured Image) บอกได้เลย เดี๋ยวผมกางแผนผังการเดินสาย Node ให้ครับ!