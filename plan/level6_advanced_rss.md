# 🔓 Level 6: ล้วงลึก RSS Feed (Manual Parsing)

พี่ตาไวมากครับ! Link ข่าวจริงๆ มันซ่อนอยู่ใน Tag ลับที่ชื่อ **`ht:news_item`**
แต่ Node RSS แบบปกติมันมองไม่เห็น เพราะมันอ่านแค่ Tag มาตรฐาน

ดังนั้น เราต้องใช้วิชา "แกะกล่อง XML" เองครับ (Advanced) 💪

---

## 🛠️ ขั้นตอนการเจาะเอา Link ข่าว (ทีละ Step)

### Step 1: เปลี่ยนมาใช้ HTTP Request อ่าน Feed
ลบ Node **RSS Read** เดิมออก (หรือ Disable ไว้) แล้วเปลี่ยนมาใช้:

1. **HTTP Request**
   - **Method:** `GET`
   - **URL:** `https://trends.google.com/trending/rss?geo=TH`
   - **Response Format:** `String` (สำคัญ! เราจะเอา Text ดิบๆ มาแกะเอง)

2. **XML** (Node สำหรับแกะภาษา XML)
   - ต่อจาก HTTP Request
   - **Mode:** `Convert XML to JSON`
   - **JSON Property Name:** `data`

### Step 2: จัดระเบียบข้อมูล (Item Lists)
3. **Item Lists** (เอามาแตก Array)
   - **Field to Split Out:** `data.rss.channel.item`
   - ตอนนี้เราจะได้ 20 ข่าวเรียงกันสวยๆ

### Step 3: ดึง Link จริง (Code Node)
4. **Code** (JavaScript)
   - เราจะดึง Link ข่าวอันแรกสุด (News Item 1) ของแต่ละหัวข้อครับ
   - ใส่โค้ดนี้:
     ```javascript
     const link = item.json['ht:news_item'][0]['ht:news_item_url'];
     // บางทีมันอาจจะมาเป็น Array หรือ Object ต้องระวัง
     
     return {
       json: {
         title: item.json.title,
         real_link: link
       }
     }
     ```
   *(เดี๋ยวผม Test โค้ดละเอียดให้หน้างานอีกที)*

### Step 4: ส่งไปดูดเนื้อหา! (Scraper)
5. **HTTP Request** (รอบที่ 2)
   - **URL:** `{{ $json.real_link }}`
   - **Response Format:** `String`

6. **HTML to Markdown**
   - แปลง HTML เน่าๆ ให้เป็น Text สวยๆ

7. **AI Agent**
   - Prompt: *"สรุปข่าวจากเนื้อหานี้ และเขียนใหม่ให้น่าอ่าน..."*

---

**สรุป:** แผนนี้ **ทำได้จริง 100%** ครับ แต่ต้องรื้อช่วงต้นน้ำ (RSS Node) นิดหน่อย
พี่พร้อมลุยแผน **"เจาะไข่แดง XML"** นี้มั้ยครับ? ถ้าพร้อม ผมจะเขียน Guide แบบจับมือทำให้เลย!
