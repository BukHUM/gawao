# 📘 คู่มือโปรเจกต์: สร้าง AI นักข่าวทันกระแส (Level 5 - Extra)

ยินดีต้อนรับสู่ Level พิเศษ! ตอนนี้ข่าวเรามีแต่ตัวหนังสือ มันดูแห้งแล้งไปหน่อย เรามาเพิ่ม **"รูปภาพประกอบ** สวยๆ จาก **Unsplash** กันดีกว่าครับ 📸

---

## 🎯 Level 5: ช่างภาพ (Visuals)

### 1. สิ่งที่ต้องเตรียม (Unsplash API) 🔑
เราจะใช้ Unsplash เพราะรูปสวยและฟรี แต่ต้องขออนุญาตใช้ API ก่อนครับ:

1. 🌐 ไปที่ [Unsplash Developers](https://unsplash.com/developers)
2. **Login/Register** (ใช้บัญชี Facebook หรือ Email ก็ได้)
3. กดปุ่ม **New Application**
4. ยอมรับเงื่อนไข (Accept terms)
5. **Name:** ตั้งชื่ออะไรก็ได้ (เช่น `My News Bot`)
6. เลื่อนลงมาหาหัวข้อ **Keys** > ก๊อปปี้ **"Access Key"** เก็บไว้ (นี่คือกุญแจของเรา!)

---

## 2. ขั้นตอนการสร้าง (Step-by-Step)

### Step 1: ปรับเส้นทางเดินเรือ (Workflow) 🗺️
เราจะแทรกช่างภาพ (Unsplash) เข้าไปก่อนส่งงานให้บรรณาธิการตรวจครั้งสุดท้าย (Code)

1. **ลบเส้น** เดิมที่เชื่อมระหว่าง `Basic LLM Chain` กับ `Code (Parse JSON)` ออก
2. เพิ่ม Node **Unsplash** เข้าไปตรงกลาง
3. ลากเส้นใหม่ให้เรียงกันแบบนี้:
   `Basic LLM Chain` ➡️ **Unsplash** ➡️ `Code (Parse JSON)` ➡️ `WordPress`

> **💡 หา Node "Unsplash" ไม่เจอ?**
> ถ้าพิมพ์หาแล้วไม่เจอ ให้ใช้ **HTTP Request** แทนครับ (วิธีลูกทุ่งแต่ชัวร์):
> **💡 วิธีตั้งค่า HTTP Request (ตามรูปเป๊ะๆ):**
> 1. **Authentication:** เปลี่ยนจาก `Predefined Credential Type` เป็น **`Generic Credential Type`**
> 2. **Generic Credential Type:** เลือก **`Header Auth`**
> 3. **Credential for Header Auth:** กด **Create New**
>    - **Name:** `Authorization`
>    - **Value:** `Client-ID YOUR_ACCESS_KEY` (เปลี่ยน `YOUR_ACCESS_KEY` เป็นรหัสที่ได้จากเว็บ Unsplash **ต้องมีเว้นวรรค 1 ทีหลัง Client-ID**)
> 4. **Send Query Parameters:** (เลื่อนลงมาด้านล่าง)
>    - กด **Add Parameter**
>    - **Name:** `query`
>    - **Value:** `{{ $('Loop Over Items').first().json.title }}` (ต้องใช้ **Title** จาก Loop นะครับ ห้ามใช้ Output จาก AI เพราะมันยาวเกินไปจน error "URI Too Long")
>    - กด **Add Parameter** อีกอัน
>    - **Name:** `per_page`
>    - **Value:** `1`
> 5. **Output:** สำคัญ! ตอนดึงค่าต้องใช้ `{{ $json.results[0].urls.regular }}` นะครับ (เพราะโครงสร้างมันต่างกับ Node ปกตินิดหน่อย)

### Step 2: ตั้งค่า Unsplash
1. **Credentials:** เลือก Create New > ใส่ **Access Key** ที่ได้มา
2. **Resource:** `Image`
3. **Operation:** `Search`
4. **Query:** กดเฟือง ⚙️ > เลือก Expression > ใส่ `{{ $('Loop Over Items').first().json.title }}`
   * *สำคัญ:* ต้องใช้ Title ข่าวเท่านั้น ห้ามใช้เนื้อหาข่าวไม่งั้นจะ Error ครับ
5. **Limit:** `1` (เอาแค่รูปเดียวพอ)

### Step 3: เขียนโค้ดรวมร่าง (Update Code Node) 🧬
ที่ Node **Code (Parse JSON)** ให้ลบโค้ดเก่าทิ้ง แล้วใส่โค้ดชุดใหม่นี้แทนครับ
(โค้ดนี้ฉลาดขึ้น คือมันจะไปดึงข้อความจาก Basic LLM Chain มาเอง ไม่ต้องกลัวข้อมูลหาย)

```javascript
// 1. ดึงภาพจาก Unsplash (Input ที่ส่งเข้ามาล่าสุด)
// แต่ต้องเช็คก่อนว่ามีรูปมั้ย (เผื่อหาไม่เจอ)
const unsplashItem = items.length > 0 ? items[0].json : {};
const imageUrl = unsplashItem.urls?.regular || ''; 
const imageCredit = unsplashItem.user?.name || 'Unsplash';

// 2. ดึงเนื้อหาข่าวจาก Node "Basic LLM Chain" (ย้อนกลับไปเอาข้อมูลเก่า)
// สำคัญ: ต้องสะกดชื่อ Node ให้ตรงเป๊ะๆ นะครับ!
const editorNode = $('Basic LLM Chain').first(); 
const aiResponse = editorNode.json.text || editorNode.json.output || editorNode.json.content || '';

// 3. แปลง JSON และรวมร่าง
let contentObj = {};
try {
   // Clean Code Block ถ้ามี
   const cleanJson = aiResponse.replace(/```json/g, '').replace(/```/g, '').trim();
   contentObj = JSON.parse(cleanJson);
} catch (e) {
   contentObj = { title: "Error Parsing AI", content: aiResponse };
}

// 4. แทรกรูปภาพ (ถ้ามี)
if (imageUrl) {
   // ใส่ <img> ไว้บนสุดของเนื้อหา พร้อมเครดิต
   contentObj.content = `
     <img src="${imageUrl}" alt="${contentObj.title}" style="width:100%; border-radius:8px; margin-bottom:15px;" />
     <br><small style="color:gray;">Photo by ${imageCredit} on Unsplash</small>
     <br><br>
     ${contentObj.content}
   `;
}

return [{ json: contentObj }];
```

---

## 🚀 ทางเลือกที่ 2: ใช้ Google Search หาภาพ (แนะนำ! ✅)
ถ้า Unsplash หาไม่เจอ เพราะข่าวเราใหม่เกินไป หรือเฉพาะทางมาก ให้ใช้ **Google Search** เดิมที่มีอยู่ครับ หาง่ายกว่าเยอะ!

### Step 1: เพิ่ม Node Google Custom Search
1. เพิ่ม Node **Google Custom Search** (ตัวเดียวกับ Level 2)
2. **Credential:** เลือกอันเดิมที่เคยตั้งไว้ (Google Custom Search API)
3. **Resource:** `Google Custom Search`
4. **Operation:** `Search`
5. **Search Query:** `{{ $('Loop in case more than one').first().json.title }}` (ใช้ Title ข่าว)
6. **Options:** (เลื่อนลงมาล่างสุด)
   - กด **Add Option**
   - เลือก **Search Type**: `image` (สำคัญมาก! ต้องเลือกเป็น image)
   - เลือก **Date Restrict**: `d1` (เอาภาพใหม่ๆ)
   - เลือก **Rights**: `cc_publicdomain` (ถ้ากังวลเรื่องลิขสิทธิ์ ให้ใส่ค่านี้เพื่อหาภาพฟรี Public Domain)

### Step 2: แก้ Code (Parse JSON)
แก้โค้ดให้ดึง Link รูปจาก Google แทน Unsplash:

```javascript
// 1. ดึงภาพจาก Google Image (Input ที่ส่งเข้ามาล่าสุด)
const googleItem = items.length > 0 ? items[0].json : {};
const imageUrl = googleItem.link || ''; // Google ส่งมาเป็น .link
const imageCredit = googleItem.displayLink || 'Google Search';

// 2. ดึงเนื้อหาข่าวจาก Node "Basic LLM Chain"
const editorNode = $('Basic LLM Chain').first(); 
const aiResponse = editorNode.json.text || editorNode.json.output || editorNode.json.content || '';

// 3. แปลง JSON
let contentObj = {};
try {
   const cleanJson = aiResponse.replace(/```json/g, '').replace(/```/g, '').trim();
   contentObj = JSON.parse(cleanJson);
} catch (e) {
   contentObj = { title: "Error Parsing AI", content: aiResponse };
}

// 4. แทรกรูปภาพ
if (imageUrl) {
   contentObj.content = `
     <img src="${imageUrl}" alt="${contentObj.title}" style="width:100%; border-radius:8px; margin-bottom:15px;" />
     <br><small style="color:gray;">Source: ${imageCredit}</small>
     <br><br>
     ${contentObj.content}
   `;
}

return [{ json: contentObj }];
```

---
2. รอดูผลงานที่ Node **Create a post**
3. ในช่อง Content ถ้าเห็นโค้ด `<img src="https://images.unsplash.com/...">` โผล่มา แสดงว่าสำเร็จ!

---

## ⚠️ ปัญหาที่พบบ่อย
*   **หา Node ไม่เจอใน Code:** ถ้ามันฟ้องว่า `Problem getting item from node...` แปลว่าชื่อ Node ในวงเล็บ `$('Basic LLM Chain')` ไม่ตรงกับชื่อจริงใน Canvas ให้เช็คชื่อ Node นักเขียนของคุณว่าชื่ออะไร (เช่น `Editor Agent` หรือ `Chain`) แล้วแก้ในโค้ดให้ตรงกันครับ

**ขอให้สนุกกับเว็บข่าวที่มีสีสันนะครับ! 🌈**