เยี่ยมครับ! ไฟกำลังแรง มาต่อกันที่ **Level 3** เลยครับ

ในเลเวลที่แล้ว "นักสืบ" ของเราหาข้อมูลมาวางกองไว้ให้แล้ว แต่ถ้าเอาข้อมูลดิบพวกนั้นไปโพสต์เลย คนอ่านคงงงแน่ๆ หน้าที่ของเราในตอนนี้คือสวมวิญญาณ **"บรรณาธิการ"** เพื่อปรุงแต่งเนื้อหาให้น่าอ่าน พาดหัวต้องโดน และจัด Format ให้สวยงามครับ


* *เทคนิค:* การสั่งให้ตอบเป็น JSON จะช่วยให้เราแยก "ชื่อเรื่อง" กับ "เนื้อหา" ออกจากกันได้ง่ายใน Level ถัดไปครับ

---

### 4. แจก Workflow Code (Level 3 Template)

ก๊อปปี้ Code นี้ไปวางต่อจาก Node Agent ใน Level 2 ได้เลยครับ

```json
{
  "meta": {
    "instanceId": "generated_by_gemini_level3"
  },
  "nodes": [
    {
      "parameters": {
        "prompt": "=คุณคือนักเขียนข่าวออนไลน์มืออาชีพ\n\nข้อมูลดิบ:\n{{ $json.output }}\n\nคำสั่ง:\nช่วยเขียนบทความข่าวจากข้อมูลดิบด้านบน โดยมีโครงสร้างดังนี้:\n1. พาดหัวข่าว (Title): ตั้งให้ดึงดูด น่าคลิกอ่าน และมี Emoji\n2. เนื้อหา (Content): เขียนสรุปให้อ่านง่าย แบ่งย่อหน้าชัดเจน ใช้ภาษาที่เป็นกันเอง\n   - ให้ Output เนื้อหาในรูปแบบ HTML (ใช้ tag <p>, <h2>, <ul>, <li>)\n\nรูปแบบ Output JSON ที่ต้องการ (Strict JSON format):\n{\n  \"title\": \"พาดหัวข่าว...\",\n  \"content\": \"เนื้อหาข่าว HTML...\"\n}\nตอบกลับมาเป็น JSON เท่านั้น ห้ามมีข้อความอื่นปน"
      },
      "id": "editor-llm",
      "name": "Editor LLM",
      "type": "@n8n/n8n-nodes-langchain.chainLlm",
      "typeVersion": 1.4,
      "position": [
        1480,
        340
      ]
    },
    {
      "parameters": {},
      "id": "json-parser",
      "name": "Parse JSON",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1700,
        340
      ],
      "jsCode": "// แปลง Text ที่ AI ตอบกลับมา ให้เป็น JSON Object จริงๆ เพื่อใช้งานต่อ\nconst aiResponse = items[0].json.text;\nlet parsedData;\n\ntry {\n  // พยายาม Clean ข้อมูลเผื่อ AI ใส่ Markdown 
http://googleusercontent.com/immersive_entry_chip/1

### ทริคเพิ่มเติมสำหรับ Level 3 💡
ถ้าคุณรู้สึกว่าบทความที่ได้ยัง "ไม่แซ่บ" พอ ลองกลับไปแก้ Prompt ใน Node `Editor LLM` ดูครับ เช่น:
* *"ขอสำนวนแบบวัยรุ่น สั้น กระชับ"*
* *"เขียนแบบทางการ สำหรับข่าวธุรกิจ"*
* *"ช่วยใส่ Hashtag ต่อท้ายบทความให้ด้วย"*

ลองเล่นกับ Prompt ให้สนุก แล้วบอกผมนะครับถ้าพร้อมจะไป **Level 4** เพื่อยิงขึ้น WordPress!

```