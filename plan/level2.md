# 📘 คู่มือโปรเจกต์: สร้าง AI นักข่าวทันกระแส (Level 2)

ยินดีต้อนรับสู่ Level 2 ครับ! ในขั้นตอนนี้เราจะเปลี่ยน n8n ให้เป็น "นักสืบ" ที่สามารถเจาะลึกข้อมูลของแต่ละเทรนด์ได้

---

## 🎯 Level 2: นักสืบ AI (Research & Agent)

### 1. เฉลย Challenge จาก Level 1 ✅

คำตอบสำหรับวิธีจัดการข้อมูลที่ไหลเข้ามาทีละ 20 หัวข้อ คือ:
**B) Loop Over Items**

เพราะ AI Agent ทำงานได้ดีที่สุดเมื่อโฟกัสทีละเรื่อง การใช้ Loop จะช่วยให้ระบบหยิบคำค้นหามาทำทีละ 1 คำ จนครบทุกคำครับ

---

## 2. ผังการทำงาน (Workflow)

ภาพรวมของ Level นี้คือ:
`RSS Feed` (รับมา 20) ➡️ `Loop Over Items` (หยิบทีละ 1) ➡️ `AI Agent` (ค้นหาข้อมูล)

---

## 3. ขั้นตอนการสร้าง (Step-by-Step)

### Step 1: สร้าง Loop (เพื่อหยิบทีละข่าว)

1. ต่อเส้นจาก Node **RSS Feed Read**
2. เพิ่ม Node ชื่อ **Loop Over Items**
3. **การตั้งค่า:**
    - **Batch Size:** ตั้งเป็น `1` (เพื่อให้ส่งไปหา AI ทีละ 1 หัวข้อ)

### Step 2: สร้าง AI Agent (สมองของนักสืบ)

1. ต่อเส้นจาก Node **Loop Over Items** (ออกจาก Loop)
2. เพิ่ม Node ชื่อ **AI Agent**
3. **การตั้งค่าเบื้องต้น:**
    - **Prompt Type:** เลือก `Define below`
    - **Text:** พิมพ์คำสั่งลงไป โดยดึงตัวแปรจาก RSS มาใส่ เช่น:
      ```
      ช่วยค้นหาข้อมูลล่าสุดและสรุปข่าวเกี่ยวกับหัวข้อนี้ให้หน่อย: {{ $json.title }}
      ```

### Step 3: เติมอาวุธให้ Agent (Model + Tools)

Node AI Agent จะทำงานไม่ได้ถ้าขาด 2 สิ่งนี้ (สังเกตจุดเชื่อมต่อ Input ด้านซ้ายของ Node):

**1. Model (สมอง):**
- ค้นหา Node **Google Gemini Chat Model** (หรือ OpenAI) แล้วลากเส้นไปเชื่อมที่ช่อง **Model** ของ Agent
- ต้องใส่ API Key ของ Gemini ตรงนี้

**2. Tools (เครื่องมือค้นหา):**
- ค้นหา Node **Google Custom Search** (บางเวอร์ชั่นอาจชื่อ **Google Search** หรือ **Google**)
- **จุดสังเกต:** ต้องเป็น Node ที่มีแปะป้ายว่า **"Tool"** หรือไอคอนเป็นรูป **จิ๊กซอว์/ประแจ** (ไม่ใช่ Node Google ธรรมดา)
- ลากเส้นไปเชื่อมที่ช่อง **Tool** ของ Agent

> **💡 ถ้าหาไม่เจอจริงๆ (Install Community Node):**
> 1. ไปที่เมนู **Settings** > **Community Nodes**
> 2. กดปุ่ม **Install**
> 3. พิมพ์ชื่อ Package: `@bitovi/n8n-nodes-google-search`
> 4. กดยอมรับความเสี่ยงและ Install
> 5. กลับมาค้นหา Node ชื่อ **Google Search** อีกครั้งครับ (ใช้ตัวนี้แทนได้เลย)

> **💡 เกร็ดความรู้:** ช่อง **Memory** ที่ว่างอยู่ **"ไม่ต้องใส่"** นะครับ เพราะในรอบนี้เราต้องการให้ AI ทำงานจบเป็นข่าวๆ ไป ไม่ต้องจำเรื่องเก่ามาปนครับ (Stateless)

> **💡 ทริคแก้ปัญหา "ข้อมูลเก่า/ไม่อัปเดต":**
> ถ้า AI เอาข้อมูลเก่ามาตอบ ให้แก้ 2 จุดนี้ครับ:
> 1. **Google Search Node:** หาช่อง **"Date Restrict"** (ถ้าต้องกด Add Option) แล้วใส่ค่า `d1` (แปลว่าหาเฉพาะข้อมูลใน 1 วันที่ผ่านมา) หรือ `w1` (1 สัปดาห์)
> 2. **AI Agent Prompt:** เพิ่มคำสั่งไปว่า *"You MUST use the Google Search tool to find the latest information from the past 24 hours. Do not rely on your internal knowledge."*

> **🚨 แก้ปัญหา Error: "Max iterations (10) reached"**
> อาการนี้แปลว่า AI พยายามค้นหาแล้วไม่เจอข้อมูล หรือเจอแล้วไม่พอใจ เลยค้นหาซ้ำๆ จนครบ 10 รอบ
> **วิธีแก้:**
> 1. **ปรับ Date Restrict:** บางที `d1` (1 วัน) มันโหดไปสำหรับบางข่าว ลองเปลี่ยนเป็น `w1` (1 สัปดาห์) หรือลบออก (ไม่จำกัดเวลา)
> 2. **สอน AI ให้พอ:** เพิ่มประโยคนี้ท้าย Prompt: *"If you cannot find exact information, just summarize what you found. Do not keep searching more than 3 times."*
> 3. **เพิ่มรอบ:** (แก้ขัด) ดับเบิ้ลคลิก Node **AI Agent** แล้วหาหัวข้อ **"Max Iterations"** ปรับเลขขึ้นเป็น 15 หรือ 20 (แต่ไม่แนะนำ เพราะจะเปลือง Credit)

---

## 🔑 วิธีขอ API Key และ Search Engine ID (CX)

สำหรับคนที่ไม่เคยทำมาก่อน ทำตามนี้ได้เลยครับ (ฟรี 100 ครั้ง/วัน):

**1. ขอ API Key (Google Cloud Console)**
1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/apis/credentials)
2. สร้าง Project ใหม่ (ถ้ายังไม่มี)
3. กด **+ CREATE CREDENTIALS** > เลือก **API key**
4. Copy **Key** ที่ได้เก็บไว้ (นี่คือ `API Key`)
5. *สำคัญ:* อย่าลืมกด Enable API ที่ชื่อ **"Custom Search API"** ใน Library ด้วยนะครับ ไม่งั้นจะใช้งานไม่ได้

**2. ขอ Search Engine ID (Programmable Search Engine)**
1. ไปที่ [Programmable Search Engine](https://programmablesearchengine.google.com/controlpanel/all)
2. กดปุ่ม **Add** เพื่อสร้าง Search Engine ใหม่
3. **Name:** ตั้งชื่ออะไรก็ได้
4. **Link:** ให้เลือก **"Search the entire web"** (สำคัญมาก! ต้องติ๊กเลือกอันนี้)
5. กด **Create**
6. จะเห็นรหัส **cx=...** หรือกดเข้าไปดูในเมนู Customize > **Search Engine ID** (นี่คือ `CX`)

---

## 4. Workflow Code (Level 2 Template)

ก๊อปปี้ Code นี้ไปต่อท้าย Workflow เดิม หรือวางในหน้าใหม่เพื่อดูโครงสร้างได้เลยครับ (อย่าลืมใส่ API Key ของคุณเองใน Node ที่ระบบแจ้งเตือนสีแดงนะครับ)

```json
{
  "meta": {
    "instanceId": "generated_by_gemini_level2"
  },
  "nodes": [
    {
      "parameters": {
        "batchSize": 1,
        "options": {}
      },
      "id": "loop-node",
      "name": "Loop Over Items",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3,
      "position": [
        900,
        340
      ]
    },
    {
      "parameters": {
        "promptType": "define",
        "text": "={{ $json.title }} \n\nจากหัวข้อข่าวข้างต้น ช่วยค้นหาข้อมูลล่าสุด (Search) แล้วสรุปเนื้อหาสำคัญมาให้ฉัน 3 ประเด็นหลัก เพื่อเตรียมนำไปเขียนข่าวต่อ",
        "options": {}
      },
      "id": "ai-agent",
      "name": "AI Researcher Agent",
      "type": "@n8n/n8n-nodes-langchain.agent",
      "typeVersion": 1.7,
      "position": [
        1120,
        340
      ]
    },
    {
      "parameters": {
        "modelName": "models/gemini-1.5-flash"
      },
      "id": "gemini-model",
      "name": "Google Gemini Chat Model",
      "type": "@n8n/n8n-nodes-langchain.lmChatGoogleGemini",
      "typeVersion": 1,
      "position": [
        1120,
        540
      ],
      "credentials": {
        "googlePalmApi": {
          "id": "YOUR_GEMINI_CREDENTIAL_ID",
          "name": "Google Gemini(PaLM) Api account"
        }
      }
    },
    {
      "parameters": {},
      "id": "google-search-tool",
      "name": "Google Custom Search",
      "type": "@n8n/n8n-nodes-langchain.toolGoogleCustomSearch",
      "typeVersion": 1,
      "position": [
        1280,
        540
      ],
      "credentials": {
        "googleCustomSearchApi": {
          "id": "YOUR_SEARCH_CREDENTIAL_ID",
          "name": "Google Custom Search API account"
        }
      }
    }
  ],
  "connections": {
    "Loop Over Items": {
      "main": [
        [
          {
            "node": "AI Researcher Agent",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Google Gemini Chat Model": {
      "ai_language_model": [
        [
          {
            "node": "AI Researcher Agent",
            "type": "ai_language_model",
            "index": 0
          }
        ]
      ]
    },
    "Google Custom Search": {
      "ai_tool": [
        [
          {
            "node": "AI Researcher Agent",
            "type": "ai_tool",
            "index": 0
          }
        ]
      ]
    }
  }
}
```

---

## 5. วิธีทดสอบ (Testing) 🧪

1. เชื่อมต่อ **RSS Feed** (จาก Level 1) เข้ากับ **Loop Over Items**
2. ใส่ Credential (API Key) ใน Node Gemini และ Google Custom Search ให้เรียบร้อย
3. กด **Execute Workflow**
4. รอดูผลลัพธ์ที่ Node **AI Agent**
5. คุณควรจะเห็น Agent ตอบกลับมาเป็น "เนื้อหาสรุป" ของข่าวนั้นๆ ที่เกิดจากการไป Search จริงๆ

---

## 🚧 สิ่งที่ต้องคิดต่อสำหรับ Level 3

เมื่อ Level 2 สำเร็จ เราจะได้ "ข้อมูลดิบที่ถูกย่อยแล้ว" (Research Data)

### โจทย์ต่อไป
ข้อมูลสรุปจาก Agent อาจจะยังแข็งๆ ทื่อๆ หรือสั้นเกินไป ไม่เหมาะจะเอาไปโพสต์ลงเว็บทันที เราจะทำอย่างไรให้มันกลายเป็น "บทความที่น่าอ่าน" มีพาดหัวที่ดึงดูด และจัด Format สวยงาม?

**ใบ้:** เราต้องใช้นักเขียนมือทอง (LLM Chain) มาช่วยเกลาสำนวนครับ

**พร้อมแล้วบอกนะครับ เดี๋ยวเราไปต่อ Level 3: บรรณาธิการ (Content Creation) กัน!** 🚀