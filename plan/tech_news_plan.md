# 🌐 Project: Tech News Aggregator (EN to TH)

นี่คือแผนงานใหม่สำหรับระบบ **"ดูดข่าวTech นอก มาเล่าไทย"** ครับ
วิธีนี้เสถียรกว่าเดิมมาก เพราะ RSS ของเว็บพวกนี้ (The Verge, TechCrunch) เป็นมาตรฐาน และให้ข้อมูลที่ครบถ้วนกว่า Google Trends ครับ

---

## 🗺️ ภาพรวมระบบ (Workflow Overview)

**Flow:** `Loop URLs` ➡️ `RSS Read` ➡️ `Filter (24h)` ➡️ `Scraper` ➡️ `AI Rewrite (TH)` ➡️ `WordPress`

---

## 🛠️ ขั้นตอนการสร้าง (Step-by-Step)

### Step 1: เตรียมแหล่งข่าว (Source Config)
เราจะไม่ทำ Node RSS แยก 5 อันครับ มันรก เราจะใช้การ **"Loop"** แทน
1. **Node: Edit Fields (Set Variables)**
   - สร้างตัวแปรชื่อ `rss_urls` เป็น Array ของลิ้งค์ทั้งหมด:
     ```json
     [
       "https://www.theverge.com/rss/index.xml",
       "https://www.engadget.com/rss.xml",
       "https://techcrunch.com/feed/",
       "https://feeds.arstechnica.com/arstechnica/index",
       "https://news.ycombinator.com/rss"
     ]
     ```
2. **Node: Split In Batches** (Loop)
   - สั่งให้ Loop ทีละ 1 URL เพื่อส่งไปอ่าน

### Step 2: อ่านและกรองข่าว (Fetch & Filter)
3. **Node: RSS Read**
   - ตั้งค่า **RSS Feed Read**:
     - **URL:** `{{ $json.rss_urls }}`

> **⚠️ หา RSS Feed Read ไม่เจอ?**
> ปกติมันเป็น Core Node ที่มีมาให้เลยครับ แต่ถ้าหาไม่เจอจริงๆ:
> 1. ลองพิมพ์ค้นหาแค่คำว่า **"RSS"** เฉยๆ ดูครับ
> 2. ถ้าไม่มีจริงๆ ให้ใช้ท่าไม้ตาย **HTTP Request + XML** (เหมือน Level 6 อันเก่า) แทนได้เลยครับ:
>    - **HTTP Request:** URL = `{{ $json.rss_urls }}`, Format = String
>    - **XML:** Convert XML to JSON

4. **Node: If (Filter by Date)**
   - **สำคัญมาก!** ถ้าไม่กรอง มันจะดูดย้อนหลังเป็นร้อยข่าว
   - **Conditions:** `Reference Date` (วันที่ข่าว) > `{{ $now.minus(1, 'days') }}` (เอาเฉพาะ 24 ชม. ล่าสุด)

### Step 3: ดึงเนื้อหาเต็ม (Full Content Scraper)
*Tip: RSS ส่วนใหญ่ให้มาแค่ "บทคัดย่อ" (Snippet) เราต้องตามไปดูดของจริง*
5. **Node: HTTP Request** (Fetch Article HTML)
   - **URL:** `{{ $json.link }}`
   - **Output:** HTML String
   - **On Error:** เปลี่ยนจาก `Stop Workflow` เป็น **`Continue`** (สำคัญ! เพื่อข้ามเว็บที่บล็อคเรา)
   - **Header Parameters:** (เพื่อเนียนเป็นคนใช้ Browser)
     - Name: `User-Agent`
     - Value: `Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/91.0.4472.124 Safari/537.36`

6. **Node: HTML to Markdown**
   - แปลง HTML เป็น Text เพื่อตัดโฆษณาและเมนูทิ้ง

### Step 4: นักแปลและเรียบเรียง (AI Translator & Rewriter)
7. **Node: Basic LLM Chain** (หรือ AI Agent)
   - **เพิ่มเองด้วยมือ:** กด + แล้วค้นหา "Basic LLM Chain" หรือ "LLM"
   - **Prompt (System Message):**
     ```text
     คุณคือนักข่าวสายไอทีชาวไทยที่เก่งและทันสมัยที่สุด (Persona: Tech Blogger)

     ภารกิจของคุณ:
     1. รับข้อมูลบทความภาษาอังกฤษ (Source News)
     2. แปลและเรียบเรียงใหม่เป็นภาษาไทย (Rewrite) ให้อ่านสนุก เข้าใจง่าย เหมือนเพื่อนเล่าให้เพื่อนฟัง
     3. ห้ามแปลตรงตัวแบบ Google Translate แต่ให้สรุปใจความสำคัญและใส่ลีลาการเล่าเรื่อง
     4. ตั้งชื่อหัวข้อข่าวใหม่ ให้น่าสนใจ (Clickbait นิดๆ ได้)
     5. จัด Format เป็น HTML (ใช้ <h2>, <p>, <ul>, <b>)

     Output Format JSON Only:
     {
       "title": "หัวข้อข่าวสุดปัง",
       "content": "<p>เนื้อหาข่าว...</p>"
     }
     ```
   - **Input:** `{{ $json.data }}` (จาก Node HTML to Markdown)
   - **Model:** อย่าลืมต่อกับ Anthropic / OpenAI

### Step 5: จัดการรูปภาพ (Image Handling)
*ข่าวดี: RSS พวกนี้มักจะมีรูปแถมมาใน Tag `<media:content>` หรือ `<enclosure>`*
8. **Node: Code (Extract Image)**
   - เช็คว่ามีรูปใน Feed ไหม?
   - ถ้ามี -> ใช้รูปนั้นเลย
   - ถ้าไม่มี -> ใช้ Google Image Search (Level 5) มาสำรอง

9. **Node: WordPress**
   - Post ขึ้นเว็บตามปกติ

---

## 📋 สิ่งที่ต้องทำ (Checklist)
- [ ] สร้าง Node **List of RSS URLs**
- [ ] ตั้งค่า **RSS Read** + **Date Filter**
- [ ] เพิ่มระบบ **Scraper** (HTTP Request + HTML to Markdown)
- [ ] จูน Prompt **AI** ให้แปลและเล่าเรื่องสนุกๆ
- [ ] ทดสอบระบบ

**พี่ชอบ Flow นี้มั้ยครับ? ถ้าโอเค เดี๋ยวผมพาเริ่ม Step 1 เลย!**
